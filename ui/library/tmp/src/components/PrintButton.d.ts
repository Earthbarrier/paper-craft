import * as React from 'react';
export declare type Props = {
    className?: string;
    children?: React.ReactNode;
};
/**
 * HTML anchor tag allows you to download a file from the same origin.
 * This is a workaround to download a file served from a different origin
 */
export declare const PrintButton: React.FunctionComponent<Props>;
export declare function onClickPrint(): void;
//# sourceMappingURL=PrintButton.d.ts.map